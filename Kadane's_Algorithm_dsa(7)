class Solution:
    def maxSubarraySum(self, arr):
        max_sum=float("-inf")
        total=0
        for i in arr:
            total=total+i
            max_sum=max(max_sum,total)
            if total<0:
                total=0
        return max_sum